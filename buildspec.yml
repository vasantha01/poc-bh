version: 0.2
env:
    git-credential-helper: yes 
phases:  
  build:
    commands: 
      git  version
      file=`git show --pretty="format:" --name-only $CODEBUILD_RESOLVED_SOURCE_VERSION`
    - echo "$file"
      for val in $file
      if [ "val" == "compaction/*" ]
      then
      aws s3 cp $file s3://lambdagluepipedemo/compactionglue/glueartifacts/*
      aws codepipeline start-pipeline-execution --name Compaction_Glue_dem
      else
    - echo "Popat"
      fi
   
      
